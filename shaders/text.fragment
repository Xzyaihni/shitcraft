#version 330 core

out vec4 fragColor;

in vec4 vertex_color;
in vec2 tex_coordinate;

uniform sampler2D user_texture;

void main()
{
	fragColor = vec4(vertex_color.rgb, texture(user_texture, tex_coordinate).r);
}
